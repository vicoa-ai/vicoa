/**
 * Canonical outbound links for the open-source project.
 *
 * The whole Vicoa stack — CLI, daemon, backend, web, desktop and mobile — is
 * AGPLv3 in one repo, so stars, bug reports and pull requests all land in the
 * same place. Anything linking out to GitHub reads these.
 */
export const GITHUB_REPO_URL = 'https://github.com/vicoa-ai/vicoa';
export const GITHUB_ISSUES_URL = `${GITHUB_REPO_URL}/issues`;
